const numbers = [1, 2, 3, 4];
const str: string[] = [];
const strTwo: Array<string> = [];
strTwo.push('2')

interface Car {
    wheels: number;
    brand: string;
}
const cars: Car[] = [];
cars.push({brand: 'Audi', wheels: 3, type: ''})

const arrOfArr: string[][] = [];
arrOfArr.push(['he'])
